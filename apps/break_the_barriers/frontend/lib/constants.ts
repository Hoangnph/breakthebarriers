// Shared localStorage keys for the preview/pipeline UI.
// The pipeline page writes the chosen translation language under this key;
// the preview page reads it so per-page translation uses the same target.
export const TRANSLATE_LANG_KEY = "btb_translate_lang"
